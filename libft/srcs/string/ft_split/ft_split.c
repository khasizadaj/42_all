#include <stdlib.h>
#include <stdio.h>

char	**ft_split(char const *s, char c)
{
	char	**result;
	int		count;

	result = malloc(sizeof(char *) * count);
	result[0] = "";
	result[1] = "";
	result[2] = "";

	return (result);
}