#include <stdio.h>
#include <unistd.h>

void	ft_putstr(char *str);

int	main(void)
{
	// char *a = "HELLO";
	// ft_putstr(a);

	char *b = "HEY  YYYY";
	ft_putstr(b);

	// char *c = "1234578";		
	// ft_putstr(c);

	// char *c = "123004";		
	// ft_putstr(c);
}
